# Version10.2 最终版代码说明

本版本是最终提交版。它在 Version10.2 的无 `extensions/` 文件夹结构上继续修改，新增了主程序 enhancedplot 的自动启动。

## 主流程

执行：

```r
source("main.R")
```

会依次执行：

1. 读取并清洗 NHANES 数据；
2. 拟合 observed propensity score model；
3. 计算主程序 IPW ATT、VBM bootstrap R²*、MSM benchmark、VBM benchmark；
4. 写出主程序 CSV 到 `output/tables/`；
5. 写出主程序基础图到 `output/figures/`；
6. 自动调用主程序 enhancedplot：
   ```r
   v10_run_python_plot(
     script_relative_path = "scripts/enhanced_plots.py",
     input_dir = config$output_tables_dir,
     output_dir = config$main_enhanced_output_dir,
     label = "Main-program enhanced plots"
   )
   ```
7. 自动执行四个根目录 extension 脚本；
8. 四个 extension 各自自动调用对应 enhancedplot 脚本。

## 主程序 enhancedplot

主程序 enhanced 脚本是：

```text
scripts/enhanced_plots.py
```

输入：

```text
output/tables/
```

输出：

```text
output/figures_enhanced/
```

它读取主程序生成的：

```text
final_results_vs_paper.csv
plot_vbm_bootstrap_curve.csv
plot_covariate_benchmark_vbm_msm.csv
plot_vbm_benchmark_bootstrap_curve.csv
plot_msm_benchmark_bootstrap_curve.csv
```

并生成更适合报告展示的主程序 enhanced figures。

## 四个 extension

根目录下四个脚本可以由 `main.R` 自动执行，也可以单独执行：

```r
source("extension_01_hidden_strength_vbm_msm.R")
source("extension_02_vbm_ps_misspecification.R")
source("extension_03_good_overlap_instability.R")
source("extension_04_rgm_vbm_msm.R")
```

对应研究问题：

1. `extension_01_hidden_strength_vbm_msm.R`  
   不同 hidden variable 强度下 VBM 与 MSM 的表现对比。

2. `extension_02_vbm_ps_misspecification.R`  
   错误 observed PS model 设定下的 VBM 探究，采用 2 × 2 hidden-confounding / misspecification decomposition。

3. `extension_03_good_overlap_instability.R`  
   good overlap 下的 denominator 不稳定性探究，包括 hidden strength、sample size 与 epsilon robustness。

4. `extension_04_rgm_vbm_msm.R`  
   RGM/TV-L1 模型探究，并和 VBM、MSM 进行统一 benchmark 对比。

## 目录结构

```text
version10.2/
  main.R
  extension_01_hidden_strength_vbm_msm.R
  extension_02_vbm_ps_misspecification.R
  extension_03_good_overlap_instability.R
  extension_04_rgm_vbm_msm.R
  functions/
  rgm_model/
  scripts/
  data/
  output/
```

项目中不再需要 `extensions/` 文件夹。
