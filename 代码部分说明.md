# Version10.1 代码部分说明

本版本在 version9.3 基础上重构了 extension 层。主流程仍然是 NHANES 的 VBM/MSM 分析；新增的 Version10.1 逻辑不再放入 `extensions/` 文件夹，而是改为项目根目录下的四个独立脚本。

## 默认执行方式

```r
source("main.R")
```

`main.R` 会在主分析结束后直接 source 四个根目录 extension 脚本：

```r
source_project_file("extension_01_hidden_strength_vbm_msm.R")
source_project_file("extension_02_vbm_ps_misspecification.R")
source_project_file("extension_03_good_overlap_instability.R")
source_project_file("extension_04_rgm_vbm_msm.R")
```

四个 extension 分别对应：

1. `extension_01_hidden_strength_vbm_msm.R`：不同 hidden variable 强度下，VBM 与 MSM 的表现对比。
2. `extension_02_vbm_ps_misspecification.R`：错误 observed PS model 设定下的 VBM 探究。
3. `extension_03_good_overlap_instability.R`：good overlap 下的 denominator 不稳定性探究。
4. `extension_04_rgm_vbm_msm.R`：RGM/TV-L1 模型探究，并对比 MSM 与 VBM。

## 单独执行某个 extension

```r
source("extension_01_hidden_strength_vbm_msm.R")
source("extension_02_vbm_ps_misspecification.R")
source("extension_03_good_overlap_instability.R")
source("extension_04_rgm_vbm_msm.R")
```

也可以只执行四个 extension：

```r
source("run_all_version10_extensions.R")
```

## enhancedplot 自动启动

四个 enhancedplot Python 脚本统一放在 `scripts/` 下：

```text
scripts/enhancedplot_hidden_strength.py
scripts/enhancedplot_vbm_ps_misspecification.py
scripts/enhancedplot_good_overlap_instability.py
scripts/enhancedplot_rgm_vbm_msm.py
```

每个 extension 的 R 脚本末尾都会自动调用对应 enhancedplot 脚本，不需要额外手动执行 Python。

## 输出路径

所有 extension 输出统一放在：

```text
output/version10_extension_results/
```

每个 extension 下有：

```text
tables/
figures/
figures_enhanced/
```

## 开关

全部 extension 默认开启：

```r
config$run_version10_extensions = TRUE
config$run_extension_01_hidden_strength_vbm_msm = TRUE
config$run_extension_02_vbm_ps_misspecification = TRUE
config$run_extension_03_good_overlap_instability = TRUE
config$run_extension_04_rgm_vbm_msm = TRUE
```

临时关闭全部或某一个 extension，可以在执行 `main.R` 前设置 option：

```r
options(v10_run_version10_extensions = FALSE)
source("main.R")
```

或在 `functions/config.R` 中修改对应开关。
